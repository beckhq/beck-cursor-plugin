{
  "mcpServers": {
    "beck": {
      "command": "npx",
      "args": ["-y", "@becklabs/beck-mcp-server"],
      "env": {
        "BECK_API_TOKEN": "${BECK_API_TOKEN}",
        "BECK_API_URL": "${BECK_API_URL}"
      }
    }
  }
}
